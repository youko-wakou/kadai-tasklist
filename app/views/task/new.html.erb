<h1>タスクの新規作成ページ</h1>
<%= form_for @task, url: task_index_path do |f| %>
  <%= f.label :content, 'タスク' %>
  <%= f.text_field :content %>
  
  <%= f.submit '投稿' %>
<% end %>

<%= link_to '一覧に戻る',:controller => "task", :action => "index" %>